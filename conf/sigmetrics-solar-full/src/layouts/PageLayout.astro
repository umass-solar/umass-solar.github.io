---
import BaseLayout from "./BaseLayout.astro";
import Hero from "../components/Hero.astro";
import { site, nav } from "../content/site";

const { pageTitle, lead } = Astro.props as { pageTitle: string; lead?: string };
---
<BaseLayout title={`${pageTitle} · ${site.title}`} navItems={nav}>
  <Hero title={pageTitle} lead={lead} />
  <section class="container" style="padding: 22px 0 44px 0;">
    <slot />
  </section>
</BaseLayout>
